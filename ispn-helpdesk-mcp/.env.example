# ISPN Helpdesk MCP - Environment Configuration
# ===========================================================
# This file documents environment variables for the
# ISPN Helpdesk read-only query MCP for Claude Desktop.
# Copy this file to .env or configure in Claude Desktop config.

# =============================================================================
# ISPN API Configuration (REQUIRED)
# =============================================================================

# ISPN Helpdesk API Base URL
# The CGI endpoint for ISPN API commands
ISPN_API_URL=https://api.helpdesk.ispn.net/exec.pl

# ISPN API Authorization Code
# Unique access code provided by ISPN for API authentication
# Each user should have their own auth code for audit trail
# Obtain from your ISPN account administrator
ISPN_AUTH_CODE=your-ispn-auth-code-here

# =============================================================================
# Logging Configuration (Optional)
# =============================================================================

# Log Level: trace, debug, info, warn, error, fatal
# Default: info
LOG_LEVEL=info

# Enable pretty-printed logs (for development)
# Default: true (set to false for JSON logs in production)
LOG_PRETTY=true

# =============================================================================
# Claude Desktop Configuration Example
# =============================================================================
# Add this to your Claude Desktop config file:
# macOS: ~/Library/Application Support/Claude/claude_desktop_config.json
# Windows: %APPDATA%\Claude\claude_desktop_config.json
# Linux: ~/.config/Claude/claude_desktop_config.json
#
# {
#   "mcpServers": {
#     "ispn-helpdesk": {
#       "command": "node",
#       "args": [
#         "/absolute/path/to/ispn-helpdesk-mcp/dist/mcp-server.js"
#       ],
#       "env": {
#         "ISPN_API_URL": "https://api.helpdesk.ispn.net/exec.pl",
#         "ISPN_AUTH_CODE": "your-unique-auth-code",
#         "LOG_LEVEL": "info"
#       }
#     }
#   }
# }

# =============================================================================
# Security Notes
# =============================================================================
# 1. Never commit .env file to version control
#    - Add .env to .gitignore
#    - Use .env.example as template only
#
# 2. Each user should have their own ISPN_AUTH_CODE
#    - Contact ISPN administrator to request auth code
#    - Never share auth codes between users
#
# 3. Protect your configuration files
#    - chmod 600 .env (read/write for owner only)
#    - Never share .env or config files via email/chat
#
# 4. The MCP is READ-ONLY
#    - No modifications can be made through this MCP
#    - Safe for support staff to query customer information
#    - No risk of accidental data changes

# =============================================================================
# Quick Start
# =============================================================================
# 1. Build the MCP:
#    npm install
#    npm run build
#
# 2. Get your ISPN auth code from administrator
#
# 3. Configure in Claude Desktop (see example above)
#
# 4. Restart Claude Desktop
#
# 5. Test by asking Claude:
#    "Look up customer with billing ID 999"
#    "List all ticket categories"
#    "Check if email customer@example.com exists"

# =============================================================================
# Available Tools
# =============================================================================
# Customer Queries:
#   - ispn.customer.lookup (by billid/phone/email)
#   - ispn.customer.get_details (full customer info)
#   - ispn.customer.list_tickets (customer's ticket history)
#
# Ticket Queries:
#   - ispn.ticket.search (search by date range)
#   - ispn.escalation.list (list escalations)
#   - ispn.escalation.get (get escalation details)
#
# Network Queries:
#   - ispn.dhcp.list (DHCP reservations)
#
# Verification & Metadata:
#   - ispn.query.list_categories (available categories)
#   - ispn.account.check (check username/email existence)
